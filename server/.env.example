# Optional. If unset or connection fails, server uses .runtime/saves JSON.
# Example after creating role/db in pgAdmin:
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@127.0.0.1:5432/gaaacha
DATABASE_URL=
